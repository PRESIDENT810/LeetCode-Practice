import java.util.*;

public class main {
}

class Solution {
    public int lengthOfLongestSubstring(String s) {
        int inside = 0; // length of no-duplicate substring inside the string
        int inside_start = 0;
        int inside_end = 0;

        int last = 0; // length of no-duplicate substring on the last of the string
        int last_start = 0;
        int last_end = 0;

        Map inside_map = new HashMap();
        Map last_map = new HashMap();

        for (int idx=0; idx<s.length(); idx++){

            }
        }
    }
}
